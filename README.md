# SPI-Protocol
This project implements the SPI protocol in SystemVerilog, where the Master sends data serially using MOSI, SCLK, and CS, and the Slave receives it and converts it back to parallel form. A testbench is included to verify correct data transfer between Master and Slave.
